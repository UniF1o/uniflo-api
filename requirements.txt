# Direct dependencies only. Transitive deps are resolved by pip.
# Do not regenerate via `pip freeze` - that produces UTF-16 + transitive bloat.
# To upgrade a pin, edit it here and run `pip install -r requirements.txt --upgrade`.

# Web framework
fastapi==0.135.3
uvicorn[standard]==0.43.0
python-multipart==0.0.26
slowapi==0.1.9

# Data / ORM
sqlmodel==0.0.38
SQLAlchemy==2.0.49
psycopg2-binary==2.9.11
alembic==1.18.4

# Validation / settings
pydantic==2.12.5
pydantic-settings==2.13.1
email-validator==2.3.0
python-dotenv==1.2.2

# Auth (RS256/ES256 JWTs from Supabase JWKS)
PyJWT==2.12.1
cryptography==46.0.7

# External services
supabase==2.28.3
sentry-sdk==2.57.0

# Automation (Phase 3) — browser binaries are installed separately via
# `playwright install --with-deps chromium` (not pulled in by pip).
playwright==1.60.0

# AI field-mapping (Phase 3) — provider-agnostic. Gemini is the default
# provider; the Anthropic adapter is the parity/fallback. Both SDKs are
# lazy-imported so the modules import even if a backend isn't installed.
google-genai==2.8.0
anthropic==0.105.2
